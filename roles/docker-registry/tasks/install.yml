# tasks/install.yml
#
# Docker registry installation
---

- name: Docker registry installation
  command: docker run -d -p {{ docker_registry_ports }} --name registry registry:2
  when: "{{ docker_registry == 'true' }}"
  ignore_errors: yes
  tags:
    - packages

